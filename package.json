{
  "name": "html-playground",
  "version": "1.0.0",
  "description": "A simple html, js, css repo to mess around in",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec 'http-server -c-1'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
